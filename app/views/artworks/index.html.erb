<h1> This is an index</h1>
<%= link_to 'New', new_artwork_path %>
<br>

<% @artworks.each do |artwork| %>
	<%= link_to (image_tag artwork.image.url(:medium)), artwork %>
	<%= link_to artwork.caption, artwork %>
	<%= pluralize(artwork.comments.count, "Comment") %>
	
<% end %>


	